### Added
- Per-ticket changelog fragments in `changelog.d/` plus a zero-dependency release assembler (`scripts/assemble_changelog.py`) that concatenates fragments into `CHANGELOG.md` under a dated version heading and removes them on release, so concurrent sessions no longer edit a shared `[Unreleased]` list (MAK-152).
