# SPAR environment template. Copy to .env and fill locally. Never commit a real .env.
#
# SPAR uses the claude-agent-sdk, which authenticates via your ambient Claude Code
# session or an Anthropic API key. Set one locally only if you are not using Claude
# Code's own auth. Value here is a placeholder referenced by name only.
ANTHROPIC_API_KEY=__set_locally__
